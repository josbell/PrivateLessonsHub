import { DaySelectedPipe } from './day-selected.pipe';

describe('DaySelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new DaySelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
